---
import Layout from "../layouts/Layout.astro";
import TerminalContainer from "../components/TerminalContainer.astro";
import Startup from "../components/Startup.jsx";
const loading = false;
---

<Layout title="Farhan's GNU/Bash">
  {loading ? <Startup client:load /> : <TerminalContainer />}
</Layout>